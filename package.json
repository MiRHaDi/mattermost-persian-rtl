{
  "name": "mattermost-persian-rtl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A clean-room Mattermost webapp plugin for safer Persian and bidirectional text rendering.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify:docs": "node scripts/verify-docs.mjs",
    "verify:bundle": "node scripts/verify-bundle.mjs",
    "check": "npm run typecheck && npm run test && npm run verify:docs && npm run build && npm run package && npm run verify:bundle"
  },
  "devDependencies": {
    "@types/node": "24.10.0",
    "esbuild": "0.28.2",
    "jsdom": "30.0.1",
    "tar": "7.5.22",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
